# Ejemplo de Regresión y Matrices
X = [[1]; [2]; [3]; [4]];
Y = [[2]; [4]; [6]; [8]];

# Operación de matriz
X_T = transpuesta(X);
mostrar("Transpuesta de X:");
mostrar(X_T);

# Predicción con Perceptrón
pred = perceptron(X, Y);
mostrar("Resultado Perceptrón:");
mostrar(pred);

# Condicional
si (1 == 1) {
    mostrar("Lógica de control funcionando");
    graficar(X, Y);
}
